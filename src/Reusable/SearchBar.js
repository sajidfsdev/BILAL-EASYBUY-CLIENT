import React from "react";
import Row from "./../UI/Row/ELXRow";

const SearchBar = (props) => {
  //styles init....

  return (
    <React.Fragment>
      <Row>Search Bar Under Construction</Row>
    </React.Fragment>
  );
}; //...................

SearchBar.defaultProps = {};

export default SearchBar;
